const Events = () => {
    const handleMyEvent = (e) => {

    };
    return(
        <div>   
            <div>
                <button onClick={handleMyEvent}>Login</button>
            </div>
        </div>
    );
};

export default Events;