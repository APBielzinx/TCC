import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    imageBanner:{
        width:'90%',
        height:170,
        marginTop:15,
        borderRadius:10,
        marginLeft: 20
    },

    textBanner:{
        fontSize:30,
        color: 'white',
        marginTop: 15,
        marginLeft: -180,
        fontWeight:'bold'

    }

})

export default styles;